# 本地生成产物

这个目录只存放可重新生成的本地输出，例如测试日志、截图、回放、临时房间、内容包和一次性检查结果。除本说明外，目录内容均由 `.gitignore` 排除，可以随时删除。

正式输入应放在以下位置：

- 卡牌、协议与美术源文件：`Content/Source/`
- 客户端实际读取的编译内容：`Content/Generated/`
- 自动化测试固定输入：`tests/Fixtures/`
- 可重复使用的开发工具：`tools/`

部分验收脚本会直接在 `artifacts/` 下创建带阶段编号的目录或文件；运行相应检查后它们会自动重新出现。
